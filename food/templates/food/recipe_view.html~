{% extends 'food/base.html' %}
{% block title %}Recipes{% endblock %}

{% block body %}
    <style>
        .nav-pills > li.active > a, .nav-pills > li.active > a:focus {
            color: white;
            background-color: #FF847C;
        }

        .nav-pills > li.active > a:hover {
            background-color: #FD6378;
            color:white;
        }

    	.panel-body {
    	    background:#ffffff;
        }
    </style>


    <div class="col-sm-offset-1 col-sm-10">

		

	    <div class="col-sm-8 col-md-9">
	       <ul class="nav nav-pills" style="margin-bottom: 10px;">
	           {% for cat, cat_en in all_cook_cat %}
	               <li role="presentation" class="active" ><a href='http://127.0.0.1:8000/food/recipe/cook_cat/{{cat_en}}'>{{cat}}</a></li>
	           {% endfor %}
	       </ul>
	    </div>

    
		<div class="col-sm-12">
			<div class="panel panel-default">
			    <div class="panel-body">
			        <table class="table">



			            <tbody>
			                {% for food in all_data %}
							    <thead>
							        <tr>
										<td bgcolor="F2F2F2"> 
											<a href='http://127.0.0.1:8000/food/{{food.pk}}'> 
												<font color="#6a6b6d"><b>{{ food.cook_name }}</b></font>
											</a>
										</td>
									</tr>
							    </thead>

						        <tr>
							   		<td>

							            <div style="white-space:pre"><font color="black">{{ food.info_text|linebreaks }}</font></div>
							            {% if food.pic_file %}
							                <img src="{{ food.pic_file.url }}" class="img-responsive">
							            {% endif %}
										</br></br>
					            	</td>
						        </tr>
			                {% endfor %}
			            </tbody>
			        </table>
			    </div>
			</div>
		</div>



    </div>
{% endblock %}
